<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head.ejs') %>

    <body>
        <%- include('./partials/nav.ejs') %>
            <header>
                <h1>All Blogs
                </h1>
            </header>
            <h2>Our Menu</h2>
            <div class="content">
                <% if(blogs.length> 0){ %>
                    <% for (let index=0; index < blogs.length; index++) { %>
                        <a href="/blogs/<%= blogs[index]._id %>" class="single">
                            <div class="box">
                                <h3>
                                    <%= blogs[index].title %>
                                </h3>
                                <p>
                                    <%= blogs[index].snippet %>
                                </p>

                            </div>
                        </a>
                        <% }%>
                            <% }else {%>
                                <h3>There's nothing on currently on the Menu</h3>
                                <% } %>
            </div>
    </body>

</html>